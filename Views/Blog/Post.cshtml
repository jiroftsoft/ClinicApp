@model ClinicApp.ViewModels.CMS.BlogPostDetailsViewModel
@{
    ViewBag.Title = Model.MetaTitle ?? Model.Title;
    ViewBag.MetaDescription = Model.MetaDescription ?? Model.Summary;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link rel="stylesheet" href="@Url.Content("~/Content/css/blog-section.css")" />
<link rel="stylesheet" href="@Url.Content("~/Content/css/blog-pages.css")" />

<main class="blog-page-wrap" role="main" aria-label="مقاله">
    <div class="container">
        <article class="blog-post-article" itemscope itemtype="https://schema.org/Article">
            @if (!string.IsNullOrEmpty(Model.ImageUrl))
            {
                <div class="blog-post-hero">
                    <img src="@Model.ImageUrl" alt="@Model.Title" loading="eager" itemprop="image" />
                </div>
            }

            <div class="blog-post-body">
                <div class="blog-post-meta">
                    @if (!string.IsNullOrEmpty(Model.CategoryName))
                    {
                        <span class="badge badge-primary">
                            <i class="fas fa-tag" aria-hidden="true"></i>
                            @Model.CategoryName
                        </span>
                    }
                    @if (Model.PublishedAt.HasValue)
                    {
                        <span class="badge badge-secondary">
                            <i class="fas fa-calendar-alt" aria-hidden="true"></i>
                            @ClinicApp.Helpers.PersianDateHelper.ToPersianDate(Model.PublishedAt.Value)
                        </span>
                    }
                </div>

                <h1 class="blog-post-body h1" itemprop="headline">@Model.Title</h1>

                @if (!string.IsNullOrEmpty(Model.Summary))
                {
                    <div class="blog-post-summary-box" aria-label="خلاصه مقاله">
                        <strong>خلاصه:</strong> @Model.Summary
                    </div>
                }

                <div class="blog-post-content" itemprop="articleBody">
                    @Html.Raw(Model.Content)
                </div>

                @if (!string.IsNullOrEmpty(Model.AuthorName))
                {
                    <div class="blog-post-author">
                        <i class="fas fa-user-md" aria-hidden="true"></i>
                        <strong>نویسنده:</strong> @Model.AuthorName
                    </div>
                }

                <div class="blog-post-legal" role="note" aria-label="توجه">
                    <i class="fas fa-exclamation-triangle" aria-hidden="true"></i>
                    <strong>توجه:</strong> محتوای آموزشی است و جایگزین نظر پزشک نیست. این مقالات صرفاً جهت آگاهی عمومی ارائه شده و برای تشخیص یا درمان بیماری استفاده نشود.
                </div>

                <a href="@Url.Action("Index", "Blog")" class="blog-post-back" aria-label="بازگشت به لیست مقالات">
                    <i class="fas fa-arrow-right" aria-hidden="true"></i>
                    بازگشت به لیست مقالات
                </a>
            </div>
        </article>
    </div>
</main>
