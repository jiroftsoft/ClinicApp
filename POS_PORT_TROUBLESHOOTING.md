# راهنمای عیب‌یابی پورت POS

## 🔍 مشکل شناسایی شده

### وضعیت فعلی:
- **IP دستگاه**: `192.168.1.104` ✅ (درست)
- **پورت استفاده شده**: `2155` ❌ (اشتباه!)

### چرا پورت 2155 اشتباه است؟

از روی رسید دستگاه:
- **Server IP**: `91.240.180.186`
- **Server Port**: `2155` ← این پورت برای **ارتباط دستگاه با سرور بانک** است
- **TMS Server IP**: `91.240.180.251`
- **TMS Server Port**: `8580` ← این پورت برای **ارتباط با TMS** است

```
[برنامه کلینیک] --TCP/IP--> [دستگاه POS] --TCP/IP--> [سرور بانک]
         (IP:Port)              (ServerIP:ServerPort)
     192.168.1.104:???      91.240.180.186:2155
```

**پورت 2155 روی دستگاه listen نمی‌کند** چون این پورت برای اتصال **خروجی** دستگاه به سرور بانک است، نه برای اتصال **ورودی** از PC.

---

## ✅ راه‌حل: پیدا کردن پورت صحیح PC ↔ POS

### گام 1: بررسی منوی تنظیمات دستگاه

1. وارد منوی تنظیمات شبکه دستگاه POS شوید
2. به دنبال بخش **"PC Connection"** یا **"Local Network"** یا **"SSP1126 Port"** بگردید
3. پورت معمولاً یکی از این‌هاست:
   - **5000** (رایج‌ترین)
   - **8080** (رایج)
   - **9100** (برای برخی مدل‌ها)
   - **4000** (برای برخی مدل‌ها)

### گام 2: تست پورت‌های رایج

روی ویندوز، PowerShell را باز کنید و این دستورات را اجرا کنید:

```powershell
# تست پورت 5000
Test-NetConnection 192.168.1.104 -Port 5000

# تست پورت 8080
Test-NetConnection 192.168.1.104 -Port 8080

# تست پورت 9100
Test-NetConnection 192.168.1.104 -Port 9100
```

**نتیجه موفق**: `TcpTestSucceeded: True`

### گام 3: استفاده از Telnet (اگر PowerShell در دسترس نیست)

```cmd
telnet 192.168.1.104 5000
telnet 192.168.1.104 8080
telnet 192.168.1.104 9100
```

**نتیجه موفق**: اتصال برقرار می‌شود (پنجره telnet باز می‌ماند)

### گام 4: استفاده از Nmap (پیشرفته)

```cmd
nmap -p 5000,8080,9100,4000 192.168.1.104
```

**نتیجه موفق**: پورت‌های باز نمایش داده می‌شوند

---

## 📋 چک‌لیست تشخیصی

### ✅ بررسی‌های شبکه (لایه 2/3):

```cmd
# 1. بررسی IP سیستم
ipconfig /all

# 2. Ping دستگاه
ping 192.168.1.104

# 3. بررسی ARP Table
arp -a | findstr "192.168.1.104"
```

**نتیجه مورد انتظار**:
- Ping موفق باشد (Reply from 192.168.1.104)
- MAC Address دستگاه در ARP Table باشد

### ✅ بررسی پورت (لایه 4):

```powershell
# تست پورت‌های رایج
Test-NetConnection 192.168.1.104 -Port 5000
Test-NetConnection 192.168.1.104 -Port 8080
Test-NetConnection 192.168.1.104 -Port 9100
```

**نتیجه مورد انتظار**:
- حداقل یکی از پورت‌ها `TcpTestSucceeded: True` باشد

---

## 🔧 اصلاح تنظیمات در سیستم

بعد از پیدا کردن پورت صحیح:

1. وارد بخش **مدیریت POS Terminals** شوید
2. ترمینال را ویرایش کنید
3. **Port** را به پورت صحیح تغییر دهید (مثلاً 5000 یا 8080)
4. ذخیره کنید
5. مجدداً تست اتصال را انجام دهید

---

## 📚 مراجع

- **مستندات سامان‌کیش**: برای پورت دقیق مدل دستگاه خود
- **پروتکل SSP1126**: استاندارد ارتباط PC ↔ POS
- **پشتیبانی سامان‌کیش**: در صورت نیاز به کمک

---

## ⚠️ نکات مهم

1. **پورت 2155 و 8580**: این‌ها برای ارتباط دستگاه با سرور هستند، نه برای PC
2. **فایروال**: مطمئن شوید فایروال ویندوز یا دستگاه POS پورت را مسدود نکرده است
3. **Subnet**: PC و دستگاه POS باید در یک Subnet باشند (مثلاً 192.168.1.x)
4. **Gateway**: Gateway دستگاه باید درست تنظیم شده باشد

---

## 🎯 نتیجه‌گیری

**مشکل اصلی**: استفاده از پورت سرور بانک (2155) به جای پورت PC-POS (معمولاً 5000, 8080, 9100)

**راه‌حل**: پیدا کردن پورت صحیح از منوی دستگاه یا تست پورت‌های رایج و تغییر تنظیمات

