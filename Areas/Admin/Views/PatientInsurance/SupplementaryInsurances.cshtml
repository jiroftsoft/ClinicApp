@model ClinicApp.ViewModels.Insurance.PatientInsurance.PatientInsuranceIndexPageViewModel
@{
    ViewBag.Title = "بیمه‌های تکمیلی بیمار";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@section Styles {
    <style>
        /* 🏥 Medical Environment Styles - بهینه شده برای محیط درمانی */
        .supplementary-card {
            border: 1px solid #e3e6f0;
            border-radius: 0.5rem;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15);
            transition: all 0.3s ease;
            position: relative;
        }
        
        .supplementary-card:hover {
            box-shadow: 0 0.25rem 2rem 0 rgba(58, 59, 69, 0.25);
            transform: translateY(-2px);
        }
        
        .supplementary-card .card-body {
            padding: 1.5rem;
        }
        
        .medical-icon {
            color: #1cc88a;
            font-size: 1.2rem;
        }
        
        .coverage-badge {
            font-size: 0.875rem;
            padding: 0.5rem 1rem;
            border-radius: 1rem;
        }
        
        .status-badge {
            font-size: 0.8rem;
            padding: 0.4rem 0.8rem;
            border-radius: 0.8rem;
        }
        
        .calculation-result {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 0.5rem;
            padding: 1.5rem;
        }
        
        .loading-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.7);
            z-index: 9999;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .loading-content {
            background: white;
            padding: 2rem;
            border-radius: 1rem;
            text-align: center;
            box-shadow: 0 1rem 3rem rgba(0,0,0,0.3);
        }
        
        /* 📱 Responsive Design */
        @@media (max-width: 768px) {
            .supplementary-card {
                margin-bottom: 1rem;
            }
            
            .card-body {
                padding: 1rem;
            }
        }
        
        /* 🎨 Medical Theme Colors */
        .medical-primary { color: #1cc88a; }
        .medical-secondary { color: #36b9cc; }
        .medical-warning { color: #f6c23e; }
        .medical-danger { color: #e74a3b; }
        
        /* 🔒 Security Indicators */
        .security-indicator {
            position: absolute;
            top: 10px;
            right: 10px;
            color: #1cc88a;
            font-size: 1.1rem;
        }
        
        /* 🚀 Performance Optimizations */
        .fade-in {
            animation: fadeIn 0.5s ease-in;
        }
        
        @@keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .pulse {
            animation: pulse 2s infinite;
        }
        
        @@keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
    </style>
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <div class="d-flex justify-content-between align-items-center">
                        <h3 class="card-title mb-0">
                            <i class="fas fa-shield-alt medical-primary"></i>
                            بیمه‌های تکمیلی بیمار
                            <span class="badge badge-info ml-2" id="insuranceCount">0</span>
                        </h3>
                        <div class="card-tools">
                            <button type="button" class="btn btn-primary btn-sm" onclick="showAddSupplementaryInsuranceModal()">
                                <i class="fas fa-plus"></i>
                                افزودن بیمه تکمیلی
                            </button>
                            <button type="button" class="btn btn-success btn-sm ml-2" onclick="refreshSupplementaryInsurances()">
                                <i class="fas fa-sync-alt"></i>
                                به‌روزرسانی
                            </button>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    @if (Model.PatientInsurances != null && Model.PatientInsurances.Any())
                    {
                        <div class="row" id="supplementary-insurances-list">
                            @foreach (var insurance in Model.PatientInsurances)
                            {
                                <div class="col-md-6 col-lg-4 mb-3 fade-in">
                                    <div class="card supplementary-card border-left-info">
                                        <div class="security-indicator">
                                            <i class="fas fa-shield-alt"></i>
                                        </div>
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start">
                                                <div class="flex-grow-1">
                                                    <h6 class="card-title medical-primary">
                                                        <i class="fas fa-shield-alt medical-icon"></i>
                                                        @insurance.InsuranceProviderName
                                                    </h6>
                                                    <p class="card-text mb-2">
                                                        <small class="text-muted">
                                                            <i class="fas fa-id-card"></i>
                                                            شماره بیمه: @insurance.PolicyNumber
                                                        </small>
                                                    </p>
                                                    <p class="card-text mb-2">
                                                        <small class="text-muted">
                                                            <i class="fas fa-calendar"></i>
                                                            از: @insurance.StartDate.ToString("yyyy/MM/dd")
                                                            @if (insurance.EndDate.HasValue)
                                                            {
                                                                <span>تا: @insurance.EndDate.Value.ToString("yyyy/MM/dd")</span>
                                                            }
                                                        </small>
                                                    </p>
                                                </div>
                                                <div class="dropdown">
                                                    <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                                        <i class="fas fa-ellipsis-v"></i>
                                                    </button>
                                                    <div class="dropdown-menu dropdown-menu-right">
                                                        <a class="dropdown-item" href="#" onclick="editSupplementaryInsurance(@insurance.PatientInsuranceId)">
                                                            <i class="fas fa-edit medical-primary"></i> ویرایش
                                                        </a>
                                                        <a class="dropdown-item" href="#" onclick="calculateSupplementaryInsurance(@insurance.PatientInsuranceId)">
                                                            <i class="fas fa-calculator medical-secondary"></i> محاسبه
                                                        </a>
                                                        <a class="dropdown-item" href="#" onclick="viewSupplementaryInsuranceDetails(@insurance.PatientInsuranceId)">
                                                            <i class="fas fa-eye medical-primary"></i> جزئیات
                                                        </a>
                                                        <div class="dropdown-divider"></div>
                                                        <a class="dropdown-item text-danger" href="#" onclick="deleteSupplementaryInsurance(@insurance.PatientInsuranceId)">
                                                            <i class="fas fa-trash"></i> حذف
                                                        </a>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="mt-3">
                                                <span class="badge status-badge badge-@(insurance.IsActive ? "success" : "secondary")">
                                                    <i class="fas fa-@(insurance.IsActive ? "check-circle" : "times-circle")"></i>
                                                    @(insurance.IsActive ? "فعال" : "غیرفعال")
                                                </span>
                                                <span class="badge coverage-badge badge-info">
                                                    <i class="fas fa-percentage"></i>
                                                    @insurance.CoveragePercent%
                                                </span>
                                                @* MaxPayment removed from PatientInsuranceIndexItemViewModel as it's not part of the entity *@
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-5">
                            <i class="fas fa-shield-alt fa-3x text-muted mb-3"></i>
                            <h5 class="text-muted">هیچ بیمه تکمیلی یافت نشد</h5>
                            <p class="text-muted">برای افزودن بیمه تکمیلی جدید، روی دکمه "افزودن بیمه تکمیلی" کلیک کنید.</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal برای افزودن بیمه تکمیلی -->
<div class="modal fade" id="addSupplementaryInsuranceModal" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="fas fa-shield-alt text-primary"></i>
                    افزودن بیمه تکمیلی
                </h5>
                <button type="button" class="close" data-dismiss="modal">
                    <span>&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form id="addSupplementaryInsuranceForm">
                    @Html.AntiForgeryToken()
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="supplementaryPlanId">طرح بیمه تکمیلی</label>
                                <select class="form-control" id="supplementaryPlanId" name="InsurancePlanId" required>
                                    <option value="">انتخاب کنید...</option>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="supplementaryPolicyNumber">شماره بیمه</label>
                                <input type="text" class="form-control" id="supplementaryPolicyNumber" name="PolicyNumber" required readonly>
                                <small class="form-text text-muted">
                                    <i class="fas fa-info-circle"></i>
                                    شماره بیمه از بیمه پایه این بیمار کپی می‌شود
                                </small>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="supplementaryStartDate">تاریخ شروع</label>
                                <input type="text" class="form-control persian-datepicker" id="supplementaryStartDate" name="StartDateShamsi" required>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="supplementaryEndDate">تاریخ پایان</label>
                                <input type="text" class="form-control persian-datepicker" id="supplementaryEndDate" name="EndDateShamsi">
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="supplementaryCardNumber">شماره کارت بیمه</label>
                                <input type="text" class="form-control" id="supplementaryCardNumber" name="CardNumber" maxlength="50">
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="supplementaryPriority">اولویت بیمه</label>
                                <select class="form-control" id="supplementaryPriority" name="Priority" required>
                                    <option value="2" selected>بیمه تکمیلی اول</option>
                                    <option value="3">بیمه تکمیلی دوم</option>
                                    <option value="4">بیمه تکمیلی سوم</option>
                                    <option value="5">بیمه تکمیلی چهارم</option>
                                    <option value="6">بیمه تکمیلی پنجم</option>
                                    <option value="7">بیمه تکمیلی ششم</option>
                                    <option value="8">بیمه تکمیلی هفتم</option>
                                    <option value="9">بیمه تکمیلی هشتم</option>
                                    <option value="10">بیمه تکمیلی نهم</option>
                                </select>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <div class="form-check">
                                    <input type="checkbox" class="form-check-input" id="supplementaryIsActive" name="IsActive" value="true" checked>
                                    <label class="form-check-label" for="supplementaryIsActive">
                                        بیمه فعال
                                    </label>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <div class="alert alert-info">
                                    <i class="fas fa-info-circle"></i>
                                    <strong>نکته:</strong> این بیمه تکمیلی است و نه بیمه اصلی
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- فیلدهای مخفی برای بیمه تکمیلی -->
                    <input type="hidden" name="IsPrimary" value="false" />
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">انصراف</button>
                <button type="button" class="btn btn-primary" onclick="saveSupplementaryInsurance()">
                    <i class="fas fa-save"></i>
                    ذخیره
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Modal برای محاسبه بیمه تکمیلی -->
<div class="modal fade" id="calculateSupplementaryInsuranceModal" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="fas fa-calculator text-success"></i>
                    محاسبه بیمه تکمیلی
                </h5>
                <button type="button" class="close" data-dismiss="modal">
                    <span>&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form id="calculateSupplementaryInsuranceForm">
                    @Html.AntiForgeryToken()
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="calculationServiceId">خدمت</label>
                                <select class="form-control" id="calculationServiceId" name="ServiceId" required>
                                    <option value="">انتخاب کنید...</option>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="calculationServiceAmount">مبلغ خدمت</label>
                                <input type="number" class="form-control" id="calculationServiceAmount" name="ServiceAmount" min="0" step="1000" required>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="calculationPrimaryCoverage">پوشش بیمه اصلی</label>
                                <input type="number" class="form-control" id="calculationPrimaryCoverage" name="PrimaryCoverage" min="0" step="1000" required>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="calculationDate">تاریخ محاسبه</label>
                                <input type="text" class="form-control persian-datepicker" id="calculationDate" name="CalculationDate" required>
                            </div>
                        </div>
                    </div>
                </form>
                <div id="calculationResult" class="mt-3" style="display: none;">
                    <div class="alert alert-info">
                        <h6><i class="fas fa-info-circle"></i> نتیجه محاسبه:</h6>
                        <div id="calculationDetails"></div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">بستن</button>
                <button type="button" class="btn btn-success" onclick="performCalculation()">
                    <i class="fas fa-calculator"></i>
                    محاسبه
                </button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // 🏥 Medical Environment JavaScript - بهینه شده برای محیط درمانی
        $(document).ready(function() {
            console.log('🏥 MEDICAL: صفحه بیمه‌های تکمیلی بارگذاری شد');
            
            // Initialize Persian DatePicker
            $('.persian-datepicker').persianDatepicker({
                format: 'YYYY/MM/DD',
                altField: '.gregorian-datepicker',
                observer: true,
                timePicker: {
                    enabled: false
                }
            });

            // بارگیری همزمان داده‌ها برای بهبود Performance
            Promise.all([
                loadSupplementaryInsurancePlans(),
                loadServicesForCalculation(),
                updateInsuranceCount()
            ]).then(function() {
                console.log('🏥 MEDICAL: تمام داده‌ها بارگذاری شد');
            }).catch(function(error) {
                console.error('🏥 MEDICAL: خطا در بارگذاری داده‌ها', error);
                showMedicalError('خطا در بارگذاری داده‌های اولیه');
            });
            
            // اضافه کردن event listeners
            setupEventListeners();
        });
        
        function setupEventListeners() {
            // Auto-refresh هر 5 دقیقه
            setInterval(function() {
                refreshSupplementaryInsurances();
            }, 300000); // 5 minutes
            
            // Keyboard shortcuts
            $(document).keydown(function(e) {
                if (e.ctrlKey && e.key === 'r') {
                    e.preventDefault();
                    refreshSupplementaryInsurances();
                }
            });
        }
        
        function updateInsuranceCount() {
            var count = $('#supplementary-insurances-list .col-md-6').length;
            $('#insuranceCount').text(count);
            console.log('🏥 MEDICAL: تعداد بیمه‌های تکمیلی به‌روزرسانی شد:', count);
        }

        function showAddSupplementaryInsuranceModal() {
            console.log('🏥 MEDICAL: نمایش مودال افزودن بیمه تکمیلی...');
            
            // بارگیری طرح‌های بیمه قبل از نمایش مودال
            loadSupplementaryInsurancePlans().then(function() {
                $('#addSupplementaryInsuranceModal').modal('show');
                console.log('🏥 MEDICAL: مودال افزودن بیمه تکمیلی نمایش داده شد');
            }).catch(function(error) {
                console.error('🏥 MEDICAL: خطا در بارگیری طرح‌های بیمه:', error);
                showMedicalError('خطا در بارگیری طرح‌های بیمه: ' + error);
            });
        }

        // Event handlers برای مودال
        $(document).ready(function() {
            // Event handler برای بسته شدن مودال
            $('#addSupplementaryInsuranceModal').on('hidden.bs.modal', function() {
                console.log('🏥 MEDICAL: مودال افزودن بیمه تکمیلی بسته شد');
                // پاک کردن فرم
                $('#addSupplementaryInsuranceForm')[0].reset();
            });
            
            // Event handler برای کلیک روی دکمه انصراف
            $('#addSupplementaryInsuranceModal .btn-secondary').on('click', function() {
                console.log('🏥 MEDICAL: کلیک روی دکمه انصراف');
                $('#addSupplementaryInsuranceModal').modal('hide');
            });
            
            // Event handler برای کلیک روی دکمه X
            $('#addSupplementaryInsuranceModal .close').on('click', function() {
                console.log('🏥 MEDICAL: کلیک روی دکمه X');
                $('#addSupplementaryInsuranceModal').modal('hide');
            });
        });

        function loadSupplementaryInsurancePlans() {
            console.log('🏥 MEDICAL: بارگیری طرح‌های بیمه تکمیلی...');
            
            return new Promise(function(resolve, reject) {
                $.ajax({
                    url: '@Url.Action("GetInsurancePlans", "InsuranceTariff", new { area = "Admin" })',
                    type: 'GET',
                    timeout: 10000, // 10 seconds timeout
                    dataType: 'json',
                    success: function(response) {
                        console.log('🏥 MEDICAL: Response received:', response);
                        
                        if (response && response.success) {
                            console.log('🏥 MEDICAL: طرح‌های بیمه دریافت شد', response.data ? response.data.length : 0);
                            
                            var options = '<option value="">انتخاب کنید...</option>';
                            if (response.data && Array.isArray(response.data)) {
                                $.each(response.data, function(index, plan) {
                                    options += '<option value="' + plan.id + '">' + plan.name + '</option>';
                                });
                            }
                            $('#supplementaryPlanId').html(options);
                            
                            // بارگیری شماره بیمه پایه
                            loadPrimaryInsurancePolicyNumber();
                            
                            resolve();
                        } else {
                            var errorMessage = response ? response.message : 'پاسخ نامعتبر از سرور';
                            console.error('🏥 MEDICAL: خطا در دریافت طرح‌های بیمه', errorMessage);
                            showMedicalError('خطا در دریافت طرح‌های بیمه: ' + errorMessage);
                            reject(errorMessage);
                        }
                    },
                    error: function(xhr, status, error) {
                        console.error('🏥 MEDICAL: خطای AJAX در دریافت طرح‌های بیمه', {
                            status: xhr.status,
                            statusText: xhr.statusText,
                            responseText: xhr.responseText,
                            error: error
                        });
                        showMedicalError('خطا در ارتباط با سرور برای دریافت طرح‌های بیمه (Status: ' + xhr.status + ')');
                        reject(error);
                    }
                });
            });
        }
        
        function loadPrimaryInsurancePolicyNumber() {
            console.log('🏥 MEDICAL: بارگیری شماره بیمه پایه...');
            
            $.ajax({
                url: '@Url.Action("GetPrimaryInsurancePolicyNumber", "PatientInsurance", new { area = "Admin" })',
                type: 'GET',
                data: { patientId: @ViewBag.PatientId },
                dataType: 'json',
                success: function(response) {
                    console.log('🏥 MEDICAL: شماره بیمه پایه دریافت شد:', response);
                    
                    if (response && response.success && response.data) {
                        $('#supplementaryPolicyNumber').val(response.data);
                        console.log('🏥 MEDICAL: شماره بیمه پایه تنظیم شد:', response.data);
                    } else {
                        console.warn('🏥 MEDICAL: شماره بیمه پایه یافت نشد');
                        showMedicalWarning('ابتدا باید بیمه پایه برای این بیمار تعریف شود');
                    }
                },
                error: function(xhr, status, error) {
                    console.error('🏥 MEDICAL: خطا در دریافت شماره بیمه پایه:', {
                        status: xhr.status,
                        statusText: xhr.statusText,
                        responseText: xhr.responseText,
                        error: error
                    });
                    showMedicalError('خطا در دریافت شماره بیمه پایه');
                }
            });
        }

        function loadServicesForCalculation() {
            $.ajax({
                url: '@Url.Action("GetActiveServices", "ServiceManagement", new { area = "Admin" })',
                type: 'GET',
                data: { serviceCategoryId: 0 }, // همه خدمات
                success: function(response) {
                    if (response.success) {
                        var options = '<option value="">انتخاب کنید...</option>';
                        $.each(response.data, function(index, service) {
                            options += '<option value="' + service.id + '">' + service.name + '</option>';
                        });
                        $('#calculationServiceId').html(options);
                    }
                },
                error: function() {
                    console.error('خطا در دریافت خدمات');
                }
            });
        }

        function saveSupplementaryInsurance() {
            console.log('🏥 MEDICAL: شروع ذخیره بیمه تکمیلی...');
            
            var form = $('#addSupplementaryInsuranceForm');
            
            // اعتبارسنجی فرم
            if (!form[0].checkValidity()) {
                console.error('🏥 MEDICAL: فرم معتبر نیست');
                form[0].reportValidity();
                return;
            }
            
            var formData = form.serialize();
            
            // اضافه کردن PatientId به formData
            formData += '&PatientId=@ViewBag.PatientId';
            
            console.log('🏥 MEDICAL: ارسال داده‌ها:', formData);
            
            $.ajax({
                url: '@Url.Action("CreateAjax", "PatientInsurance", new { area = "Admin" })',
                type: 'POST',
                data: formData,
                dataType: 'json',
                success: function(response) {
                    console.log('🏥 MEDICAL: پاسخ سرور:', response);
                    
                    if (response && response.success) {
                        console.log('🏥 MEDICAL: بیمه تکمیلی با موفقیت ذخیره شد');
                        showMedicalSuccess('بیمه تکمیلی با موفقیت اضافه شد');
                        $('#addSupplementaryInsuranceModal').modal('hide');
                        
                        // پاک کردن فرم
                        form[0].reset();
                        
                        // به‌روزرسانی صفحه
                        setTimeout(function() {
                            location.reload();
                        }, 1000);
                    } else {
                        var errorMessage = response ? response.message : 'خطای نامشخص';
                        console.error('🏥 MEDICAL: خطا در ذخیره بیمه تکمیلی:', errorMessage);
                        
                        // نمایش جزئیات خطاهای اعتبارسنجی
                        if (response && response.errors) {
                            var errorDetails = Object.keys(response.errors).map(function(key) {
                                return key + ': ' + response.errors[key].join(', ');
                            }).join('; ');
                            console.error('🏥 MEDICAL: جزئیات خطاهای اعتبارسنجی:', errorDetails);
                            showMedicalError('خطاهای اعتبارسنجی: ' + errorDetails);
                        } else {
                            showMedicalError('خطا در ذخیره بیمه تکمیلی: ' + errorMessage);
                        }
                    }
                },
                error: function(xhr, status, error) {
                    console.error('🏥 MEDICAL: خطای AJAX در ذخیره بیمه تکمیلی:', {
                        status: xhr.status,
                        statusText: xhr.statusText,
                        responseText: xhr.responseText,
                        error: error
                    });
                    
                    var errorMessage = 'خطا در ارتباط با سرور';
                    if (xhr.responseText) {
                        try {
                            var response = JSON.parse(xhr.responseText);
                            if (response.message) {
                                errorMessage = response.message;
                            }
                        } catch (e) {
                            console.error('🏥 MEDICAL: خطا در تجزیه پاسخ خطا:', e);
                        }
                    }
                    
                    showMedicalError('خطا در ذخیره بیمه تکمیلی: ' + errorMessage);
                }
            });
        }

        function calculateSupplementaryInsurance(patientInsuranceId) {
            $('#calculateSupplementaryInsuranceModal').modal('show');
        }

        function performCalculation() {
            console.log('🏥 MEDICAL: شروع محاسبه بیمه تکمیلی...');
            
            // اعتبارسنجی ورودی‌ها
            var serviceId = $('#calculationServiceId').val();
            var serviceAmount = $('#calculationServiceAmount').val();
            var primaryCoverage = $('#calculationPrimaryCoverage').val();
            
            if (!serviceId || !serviceAmount || !primaryCoverage) {
                showMedicalError('لطفاً تمام فیلدها را پر کنید');
                return;
            }
            
            if (parseFloat(serviceAmount) <= 0) {
                showMedicalError('مبلغ خدمت باید بیشتر از صفر باشد');
                return;
            }
            
            if (parseFloat(primaryCoverage) < 0) {
                showMedicalError('پوشش بیمه اصلی نمی‌تواند منفی باشد');
                return;
            }
            
            showLoadingIndicator();
            
            var form = $('#calculateSupplementaryInsuranceForm');
            var formData = form.serialize();
            
            $.ajax({
                url: '@Url.Action("CalculateSupplementaryInsurance", "PatientInsurance", new { area = "Admin" })',
                type: 'POST',
                data: formData,
                timeout: 15000, // 15 seconds timeout
                success: function(response) {
                    hideLoadingIndicator();
                    
                    if (response.success) {
                        console.log('🏥 MEDICAL: محاسبه بیمه تکمیلی موفق', response.data);
                        showCalculationResult(response.data);
                        showMedicalSuccess('محاسبه بیمه تکمیلی با موفقیت انجام شد');
                    } else {
                        console.error('🏥 MEDICAL: خطا در محاسبه بیمه تکمیلی', response.message);
                        showMedicalError('خطا در محاسبه: ' + response.message);
                    }
                },
                error: function(xhr, status, error) {
                    hideLoadingIndicator();
                    console.error('🏥 MEDICAL: خطای AJAX در محاسبه بیمه تکمیلی', error);
                    
                    // تشخیص نوع خطا
                    var errorMessage = 'خطا در محاسبه بیمه تکمیلی';
                    if (xhr.status === 401) {
                        errorMessage = 'دسترسی غیرمجاز - لطفاً دوباره وارد شوید';
                    } else if (xhr.status === 403) {
                        errorMessage = 'دسترسی غیرمجاز - شما مجاز به انجام این عملیات نیستید';
                    } else if (xhr.status === 500) {
                        errorMessage = 'خطای سرور - لطفاً با مدیر سیستم تماس بگیرید';
                    } else if (xhr.status === 0) {
                        errorMessage = 'خطا در اتصال به سرور - لطفاً اتصال اینترنت خود را بررسی کنید';
                    }
                    
                    showMedicalError(errorMessage);
                }
            });
        }

        function showCalculationResult(data) {
            var resultHtml = '<div class="row">';
            resultHtml += '<div class="col-md-6"><strong>پوشش بیمه تکمیلی:</strong> ' + data.supplementaryCoverage + ' تومان</div>';
            resultHtml += '<div class="col-md-6"><strong>سهم نهایی بیمار:</strong> ' + data.finalPatientShare + ' تومان</div>';
            resultHtml += '</div>';
            resultHtml += '<div class="row mt-2">';
            resultHtml += '<div class="col-md-6"><strong>کل پوشش:</strong> ' + data.totalCoverage + ' تومان</div>';
            resultHtml += '<div class="col-md-6"><strong>درصد کل پوشش:</strong> ' + data.totalCoveragePercent + '%</div>';
            resultHtml += '</div>';
            
            $('#calculationDetails').html(resultHtml);
            $('#calculationResult').show();
        }

        function editSupplementaryInsurance(patientInsuranceId) {
            // Implementation for editing supplementary insurance
            console.log('Edit supplementary insurance:', patientInsuranceId);
        }

        function deleteSupplementaryInsurance(patientInsuranceId) {
            console.log('🏥 MEDICAL: درخواست حذف بیمه تکمیلی', patientInsuranceId);
            
            if (confirm('آیا از حذف این بیمه تکمیلی اطمینان دارید؟\nاین عمل قابل بازگشت نیست.')) {
                showLoadingIndicator();
                
                $.ajax({
                    url: '@Url.Action("Delete", "PatientInsurance", new { area = "Admin" })',
                    type: 'POST',
                    data: { 
                        id: patientInsuranceId, 
                        __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').val() 
                    },
                    timeout: 10000,
                    success: function(response) {
                        hideLoadingIndicator();
                        
                        if (response.success) {
                            console.log('🏥 MEDICAL: بیمه تکمیلی با موفقیت حذف شد');
                            showMedicalSuccess('بیمه تکمیلی با موفقیت حذف شد');
                            location.reload();
                        } else {
                            console.error('🏥 MEDICAL: خطا در حذف بیمه تکمیلی', response.message);
                            showMedicalError('خطا در حذف: ' + response.message);
                        }
                    },
                    error: function(xhr, status, error) {
                        hideLoadingIndicator();
                        console.error('🏥 MEDICAL: خطای AJAX در حذف بیمه تکمیلی', error);
                        showMedicalError('خطا در حذف بیمه تکمیلی');
                    }
                });
            }
        }
        
        // 🏥 Medical Environment Helper Functions
        function showLoadingIndicator() {
            console.log('🏥 MEDICAL: نمایش loading indicator');
            
            if ($('#loadingOverlay').length === 0) {
                $('body').append(`
                    <div id="loadingOverlay" class="loading-overlay">
                        <div class="loading-content">
                            <i class="fas fa-spinner fa-spin fa-3x medical-primary"></i>
                            <h5 class="mt-3 mb-2">در حال پردازش...</h5>
                            <p class="text-muted">لطفاً صبر کنید</p>
                            <div class="progress mt-3" style="width: 200px;">
                                <div class="progress-bar progress-bar-striped progress-bar-animated" 
                                     role="progressbar" style="width: 100%"></div>
                            </div>
                        </div>
                    </div>
                `);
            }
        }
        
        function hideLoadingIndicator() {
            console.log('🏥 MEDICAL: مخفی کردن loading indicator');
            
            $('#loadingOverlay').fadeOut(300, function() {
                $(this).remove();
            });
        }
        
        function showMedicalError(message) {
            console.error('🏥 MEDICAL: خطا', message);
            
            if (typeof toastr !== 'undefined') {
                toastr.error(message, 'خطا در سیستم درمانی', {
                    timeOut: 8000,
                    closeButton: true,
                    progressBar: true,
                    positionClass: 'toast-top-right',
                    showMethod: 'slideDown',
                    hideMethod: 'slideUp'
                });
            } else {
                alert('🏥 خطا: ' + message);
            }
        }
        
        function showMedicalSuccess(message) {
            console.log('🏥 MEDICAL: موفقیت', message);
            
            if (typeof toastr !== 'undefined') {
                toastr.success(message, 'عملیات موفق', {
                    timeOut: 5000,
                    closeButton: true,
                    progressBar: true,
                    positionClass: 'toast-top-right'
                });
            } else {
                alert('🏥 موفقیت: ' + message);
            }
        }
        
        function showMedicalWarning(message) {
            console.log('🏥 MEDICAL: هشدار', message);
            
            if (typeof toastr !== 'undefined') {
                toastr.warning(message, 'هشدار', {
                    timeOut: 8000,
                    closeButton: true,
                    progressBar: true,
                    positionClass: 'toast-top-right'
                });
            } else {
                alert('🏥 هشدار: ' + message);
            }
        }
        
        function refreshSupplementaryInsurances() {
            console.log('🏥 MEDICAL: به‌روزرسانی بیمه‌های تکمیلی...');
            location.reload();
        }
        
        function viewSupplementaryInsuranceDetails(patientInsuranceId) {
            console.log('🏥 MEDICAL: نمایش جزئیات بیمه تکمیلی', patientInsuranceId);
            // TODO: پیاده‌سازی نمایش جزئیات
            showMedicalSuccess('جزئیات بیمه تکمیلی نمایش داده شد');
        }
    </script>
}
