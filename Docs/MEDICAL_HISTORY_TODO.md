# TODO — تاریخچه پزشکی پروداکشن

برگرفته از [MEDICAL_HISTORY_PRODUCTION_ROADMAP.md](./MEDICAL_HISTORY_PRODUCTION_ROADMAP.md). برای هر مورد پس از انجام، `[ ]` را به `[x]` تغییر دهید.

---

## فاز ۲ — الزام‌ها و داروها

### ۲.۱ الزام‌ها و UI

- [ ] در مودال، فیلدهای الزامی با `<span class="text-danger">*</span>` و در صورت امکان `required` و `aria-required` مشخص شوند.
- [ ] برای «تاریخ شروع» بر اساس نوع (بیماری/جراحی/دارو) یک راهنمای کوتاه (placeholder یا help text) اضافه شود.
- [ ] در ViewModel و Entity اعتبارسنجی سمت سرور برای Type و Title حفظ شود.

### ۲.۲ شدت (Severity)

- [ ] مقادیر ثابت شدت در مودال: لیست/دراپ‌داون با گزینه‌های «خفیف»، «متوسط»، «شدید»، «بحرانی» + «سایر (متنی)».
- [ ] در مدل/ViewModel در صورت نیاز طول و اعتبار مقدار «سایر» تعریف شود.
- [ ] در نمایش کارت و تایم‌لاین، پیل شدت با همان مقادیر ثابت رنگ‌دهی شود (قبلاً تا حدی انجام شده).

### ۲.۳ مدل و ذخیره دارو

- [x] **انجام شد:** جدول جدا `MedicalHistoryMedication` و `MedicalHistoryLabResult` ایجاد شدند (Entity + Config + ناوبری در MedicalHistory + DbSet). مایگریشن را خودتان اجرا کنید — راهنما: [MIGRATION_MEDICAL_HISTORY_MEDICATION_LAB.md](./MIGRATION_MEDICAL_HISTORY_MEDICATION_LAB.md).
- [x] اجرای مایگریشن توسط شما (`Add-Migration` و `Update-Database`).
- [ ] به‌روزرسانی MedicalHistoryCreateEditViewModel با فیلدهای دارو (نام دارو، دوز، واحد، نحوه مصرف، راه مصرف، اندیکاسیون، پزشک تجویزکننده).
- [ ] به‌روزرسانی MedicalRecordFactory برای Map کردن Entity ↔ ViewModel دارو.
- [ ] سرویس Create/Update تاریخچه پزشکی برای ذخیره فیلدهای دارو (یا رکوردهای Medication).
- [ ] API CreateMedicalHistory / UpdateMedicalHistory برای دریافت و برگرداندن فیلدهای دارو.

### ۲.۴ مودال — بلوک دارو

- [ ] با انتخاب نوع «دارو» در مودال، یک بلوک (section) با فیلدهای دارو نمایش داده شود (نام دارو*، دوز، واحد، نحوه مصرف، راه مصرف، تاریخ شروع/پایان، اندیکاسیون، پزشک تجویزکننده).
- [ ] اعتبارسنجی: در نوع دارو، «نام دارو» الزامی شود (Title یا فیلد جدا).
- [ ] در medical-record.js پر کردن و ارسال فیلدهای دارو در فرم و در populateMedicalHistoryForm.

### ۲.۵ نمایش دارو در لیست و تایم‌لاین

- [ ] در _MedicalHistorySection برای نوع دارو نمایش دوز و نحوه مصرف (زیرعنوان یا خط متا).
- [ ] در تایم‌لاین (normalizeTimelineItem / کارت) برای نوع دارو نمایش دوز و نحوه مصرف در metaLine یا زیرعنوان.

---

## فاز ۳ — آزمایش‌های کلیدی

### ۳.۱ مدل نتیجه آزمایش

- [ ] طراحی نهایی: جدول `MedicalHistoryLabResult` (MedicalHistoryId, LabName, Value, Unit, LabDate, ReferenceRange) یا ذخیره JSON در MedicalHistory.
- [ ] در صورت جدول جدا: Entity، Config، Migration، Repository و سرویس برای ایجاد/خواندن/حذف نتایج آزمایش.
- [ ] ViewModel و DTO برای نمایش و ارسال نتیجه آزمایش.

### ۳.۲ فرم/مودال آزمایش

- [ ] امکان «افزودن نتیجه آزمایش» به یک رکورد تاریخچه (بیماری) — مودال یا سکشن تاشو در مودال تاریخچه.
- [ ] فیلدها: نام آزمایش (مثلاً CK-MB، Troponin)، مقدار، واحد، تاریخ انجام، محدوده مرجع (اختیاری).
- [ ] API و سرویس برای ذخیره و واکشی نتایج آزمایش مرتبط با یک MedicalHistoryId.

### ۳.۳ قالب بیمار قلبی

- [ ] (اختیاری) چک‌لیست یا قالب پیشنهادی برای «بیماری قلبی»: داروهای قلبی + آزمایش‌های CK-MB / Troponin با تاریخ.
- [ ] نمایش آزمایش‌ها در کارت تاریخچه و در تایم‌لاین.

---

## فاز ۴ — آلرژی و سابقه خانوادگی

- [ ] برای نوع «آلرژی»: فیلد آلرژن (یا استفاده از Title)، نوع واکنش، شدت واکنش در مودال و مدل.
- [ ] برای «سابقه خانوادگی»: فیلد نسبت خانوادگی (پدر، مادر، خواهر/برادر و غیره) در مودال و مدل.

---

## فاز ۵ — UX و پروداکشن

- [ ] اعتبارسنجی یکپارچه سمت سرور برای هر نوع (دارو بدون نام، بیماری با تاریخ نامعتبر و غیره).
- [ ] خروجی چاپ/PDF پرونده شامل داروها و در صورت پیاده‌سازی، آزمایش‌ها.
- [ ] تست دسترسی‌پذیری و RTL برای مودال و لیست تاریخچه.
- [ ] حذف یا محدود کردن console.log در medical-record.js برای پروداکشن.

---

## خلاصه پیشنهاد ترتیب اجرا

1. **فاز ۲.۱ و ۲.۲** — الزام‌ها و شدت (سریع، بدون تغییر دیتابیس).
2. **فاز ۲.۳ و ۲.۴ و ۲.۵** — مدل دارو، مودال دارو، نمایش دارو.
3. **فاز ۳** — آزمایش‌های کلیدی و در صورت نیاز قالب قلبی.
4. **فاز ۴ و ۵** — آلرژی/خانوادگی و بهینه‌سازی نهایی UX و پروداکشن.

برای جزئیات هر فاز به [MEDICAL_HISTORY_PRODUCTION_ROADMAP.md](./MEDICAL_HISTORY_PRODUCTION_ROADMAP.md) مراجعه کنید.
