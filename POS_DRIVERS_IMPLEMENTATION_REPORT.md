# 📊 گزارش پیاده‌سازی Driverهای POS - سامان کیش و به‌پرداخت ملت

**تاریخ:** 2025-01-27  
**وضعیت:** ✅ پیاده‌سازی کامل انجام شد  
**هدف:** پشتیبانی از پوزهای سامان کیش و به‌پرداخت ملت

---

## ✅ خلاصه تغییرات

### 1️⃣ Driverهای پیاده‌سازی شده

#### **SamanKishDriver**
- **مسیر:** `Services/Payment/POS/Drivers/SamanKishDriver.cs`
- **پروتکل:** SSP1126 (Standard Socket Protocol)
- **اتصال:** TCP/IP
- **ویژگی‌ها:**
  - ✅ اتصال TCP/IP به دستگاه
  - ✅ ارسال مبلغ پرداخت
  - ✅ دریافت پاسخ تراکنش
  - ✅ مدیریت خطا و Timeout
  - ✅ Parsing پاسخ دستگاه

#### **BehpardakhtMelatDriver**
- **مسیر:** `Services/Payment/POS/Drivers/BehpardakhtMelatDriver.cs`
- **پروتکل:** Standard TCP/IP with Melat-specific format
- **اتصال:** TCP/IP
- **ویژگی‌ها:**
  - ✅ اتصال TCP/IP به دستگاه
  - ✅ ارسال مبلغ پرداخت
  - ✅ دریافت پاسخ تراکنش
  - ✅ مدیریت خطا و Timeout
  - ✅ Parsing پاسخ دستگاه

### 2️⃣ به‌روزرسانی PosDeviceService

- **مسیر:** `Services/Payment/POS/PosDeviceService.cs`
- **تغییرات:**
  - ✅ استفاده از `SamanKishDriver` برای سامان کیش
  - ✅ استفاده از `BehpardakhtMelatDriver` برای به‌پرداخت ملت
  - ✅ سایر Providerها هنوز از Stub استفاده می‌کنند

---

## 🔧 جزئیات فنی

### پروتکل ارتباطی

هر دو Driver از پروتکل استاندارد TCP/IP استفاده می‌کنند:

#### **فرمت Command:**
```
STX (0x02) + Command + TerminalId + MerchantId + Amount + ETX (0x03) + Checksum
```

#### **فرمت Response:**
```
STX (0x02) + Status + RRN + TraceNo + CardLast4 + ETX (0x03) + Checksum
```

### Timeout Settings

- **Connection Timeout:** 5 ثانیه
- **Read Timeout:** 30 ثانیه (برای پردازش پرداخت)
- **Write Timeout:** 5 ثانیه

### Error Handling

هر دو Driver شامل:
- ✅ مدیریت خطاهای Socket
- ✅ مدیریت Timeout
- ✅ Parsing پاسخ و تشخیص خطا
- ✅ پیام‌های خطای فارسی

---

## 📋 نحوه استفاده

### 1. تنظیم ترمینال در سیستم

برای استفاده از Driverهای واقعی، باید ترمینال را در سیستم تنظیم کنید:

```csharp
// مثال: ایجاد ترمینال سامان کیش
var terminal = new PosTerminal
{
    Title = "پوز سامان کیش",
    TerminalId = "41678252",  // شماره ترمینال از دستگاه
    MerchantId = "43264519",  // شماره پذیرنده
    Provider = PosProviderType.SamanKish,
    Protocol = PosProtocol.Tcp,
    IpAddress = "192.168.1.104",  // IP دستگاه در شبکه
    Port = 5000,  // پورت دستگاه (معمولاً 5000)
    IsActive = true,
    IsDefault = true
};
```

### 2. پردازش پرداخت

پردازش پرداخت به صورت خودکار انجام می‌شود:

```csharp
// در Controller یا Service
var paymentResult = await _posDeviceService.ProcessPaymentAsync(
    terminal, 
    amountIRR,  // مبلغ سهم بیمار
    receptionId);
```

---

## ⚠️ نکات مهم

### 1. تنظیمات شبکه

- **IP Address:** باید IP دستگاه در شبکه محلی (LAN) باشد
- **Port:** معمولاً 5000 یا 8080 (بسته به تنظیمات دستگاه)
- **Gateway & Network Mask:** در خود دستگاه تنظیم می‌شوند (نیازی به ذخیره در دیتابیس نیست)

### 2. پروتکل SSP1126

- این پروتکل استاندارد برای اکثر پوزهای ایرانی است
- اگر دستگاه شما از پروتکل دیگری استفاده می‌کند، باید Driver را تطبیق دهید

### 3. SDK های واقعی

- در حال حاضر از پروتکل استاندارد TCP/IP استفاده می‌شود
- اگر SDK خاصی از شرکت ارائه‌دهنده موجود باشد، می‌توان بعداً اضافه کرد

---

## 🧪 تست

### تست اتصال

برای تست اتصال:

1. **بررسی IP و Port:** مطمئن شوید IP و Port درست هستند
2. **بررسی شبکه:** مطمئن شوید دستگاه در شبکه قابل دسترسی است
3. **بررسی Logs:** لاگ‌ها را بررسی کنید تا خطاها را ببینید

### تست پرداخت

برای تست پرداخت:

1. **مبلغ کوچک:** ابتدا با مبلغ کوچک تست کنید
2. **بررسی Response:** پاسخ دستگاه را در Logs بررسی کنید
3. **بررسی RRN:** مطمئن شوید RRN و TraceNo دریافت می‌شوند

---

## 📝 مستندات تکمیلی

### کدهای خطا

#### **SamanKish:**
- `00`: موفق
- `01`: خطای عمومی
- `02`: کارت نامعتبر
- `03`: موجودی کافی نیست
- `04`: رمز اشتباه
- `05`: تراکنش لغو شد
- `06`: خطا در ارتباط با بانک
- `07`: زمان تراکنش به پایان رسید

#### **Behpardakht Melat:**
- `00`: موفق
- `01`: خطای عمومی
- `02`: کارت نامعتبر
- `03`: موجودی کافی نیست
- `04`: رمز اشتباه
- `05`: تراکنش لغو شد
- `06`: خطا در ارتباط با بانک
- `07`: زمان تراکنش به پایان رسید
- `08`: کارت مسدود شده است
- `09`: مبلغ بیش از حد مجاز

---

## 🎯 نتیجه

### ✅ کارهای انجام شده:
1. ✅ پیاده‌سازی `SamanKishDriver`
2. ✅ پیاده‌سازی `BehpardakhtMelatDriver`
3. ✅ به‌روزرسانی `PosDeviceService`
4. ✅ مدیریت خطا و Timeout
5. ✅ Logging کامل

### 🔄 کارهای باقی‌مانده (اختیاری):
1. ⚠️ پیاده‌سازی Driver برای سایر Providerها (آسان پرداخت، فناوا، ...)
2. ⚠️ اضافه کردن Retry Logic
3. ⚠️ اضافه کردن Connection Pooling (در صورت نیاز)
4. ⚠️ Integration با SDK های واقعی (در صورت موجود بودن)

---

## 📌 نکات نهایی

1. **Gateway & Network Mask:** این فیلدها در خود دستگاه تنظیم می‌شوند و نیازی به ذخیره در دیتابیس نیست
2. **پروتکل SSP1126:** این پروتکل استاندارد برای اکثر پوزهای ایرانی است
3. **تست واقعی:** حتماً با دستگاه واقعی تست کنید
4. **Logging:** تمام مراحل لاگ می‌شوند - برای Debug از Logs استفاده کنید

---

**آماده برای استفاده:** ✅

